services:
  web:
    container_name: custom_nginx
    build:
      context: ${PWD}
      dockerfile: Dockerfile
    ports:
      - "8081:80"
    networks:
      - build_network

networks:
  build_network:
    driver: bridge
    ipam:
      config:
        - subnet: 192.168.50.0/24
